# [ENGLISH]

This README is intentionally left blank.

## How to Set Up

1. Run `0_install_brew.sh` shell script.
2. After running `0_install_brew.sh`, execute: `source ~/.bashrc`
3. Then, run the scripts in order: `1_prerequisite.sh`, `2_clone_and_initial_patch.sh`, `3_build.sh`

---

# [KOREAN / 한글]

이 README는 의도적으로 비워두었습니다.

## 설정 방법

1. `0_` 셸 스크립트를 실행하세요.
2. `0_` 실행 후, `source ~/.bashrc`를 입력하세요.
3. 그 다음, `1_`, `2_`, ... 순서대로 스크립트를 실행하세요.
